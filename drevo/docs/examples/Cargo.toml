[package]
name = "drevo-doc-examples"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
async-trait = "0.1"
color-eyre = "0.6"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
uniqify = "0.1.0"
drevo = { path = "../.." }
